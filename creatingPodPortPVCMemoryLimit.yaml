apiVersion: v1
kind: Pod
metadata:
  name: nginx
  
spec:
  containers:
  - name: nginx
    image: nginx
    port: 
    - containerPort: 80
    volumeMounts:
    - mountPath: "/usr/share/nginx/html"
      name: task-pv-storage
    resources:
      limits:
        memory: "200Mi"
      requests:
        memory: "100Mi"
  volumes: 
  - name : task-pv-storage
    persistentVolumeClaim: 
      claimName: randomPVC
